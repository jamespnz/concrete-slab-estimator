// Stage 03 placeholder. UI terminal (React components consuming src/domain)
// is the next increment -- not part of this scaffold.
export default function App() {
  return (
    <div className="min-h-screen bg-slate-900 text-slate-100 flex items-center justify-center font-mono">
      <p>Concrete Slab &amp; Footing Estimator -- engine verified, UI pending (Stage 03).</p>
    </div>
  );
}
